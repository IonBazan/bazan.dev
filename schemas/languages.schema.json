{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Languages",
  "description": "data/resume/languages.yaml — map of language name to proficiency level. NOTE: this duplicates the `languages` block in data/resume.yaml — keep both in sync, or consolidate to one source.",
  "type": "object",
  "additionalProperties": { "type": "string" },
  "examples": [
    { "Polish": "Native", "English": "Advanced" }
  ]
}
